# Three-image X post

Upload in this order:

1. `1-inbox.png`
2. `2-pull-requests.png`
3. `3-source-control.png`

Each image is 1664 x 1664. The app capture is the unchanged original at native 1600 x 934, centered at x=32, y=365. Only the outer 14-pixel corner treatment changes screenshot edge pixels. All original text, favicons, panel context and cursors remain intact.

Post copy:

Codex and Claude Code, with the work around them. Keep active threads, pull requests, and code review together in Threadlines. Free and open source. https://www.threadlines.dev/download

Alt text:

1. Threadlines inbox showing working and waiting project threads, wrapped threads, agent conversation, and source control.
2. An open pull request beside its Threadlines conversation, with an inline review comment and CI checks popup above the composer.
3. A code diff beside the Threadlines conversation that produced it, with project threads visible in the inbox.

Production: Built-in image generation produced the blue, teal and apricot background studies. Generated app portions were rejected because their text and icons were subtly redrawn. Only the upper background bands were used in a native HTML composition. Original screenshot PNGs were then embedded without resizing. The final images are three complete views, so each remains understandable when X changes its multi-image layout.

Background prompt: A softly defocused satin color field in muted cornflower blue, ocean teal and warm apricot, with deep ink behind a centered app capture. No text, decorative headers, logos or cards. Left panel warm apricot to teal; center panel teal to cornflower; right panel cornflower to apricot.
