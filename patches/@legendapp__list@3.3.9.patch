diff --git a/react-native.web.d.ts b/react-native.web.d.ts
index c424d3ce576ea8205713d2e34bc5c0ad6de34db9..9f2abe8b8ec7fc4bbbefad87768591eb7dd9ddaa 100644
--- a/react-native.web.d.ts
+++ b/react-native.web.d.ts
@@ -224,6 +224,11 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Estimates the size of an item that has not been measured yet, in place of the running
+     * average of measured items. Return undefined to fall back to the average.
+     */
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
diff --git a/react-native.web.js b/react-native.web.js
index 5b7b716e1edb046cbb6386b417490fab793f666b..34b79d7de27f5ae9e4f4b6fc4a8879c5d705560f 100644
--- a/react-native.web.js
+++ b/react-native.web.js
@@ -1223,6 +1223,15 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  // A caller's estimate for an item that has not been measured beats the running
+  // average, which lets one tall kind of row inflate every guess for a short one.
+  const getEstimatedItemSize = state.props.getEstimatedItemSize;
+  const estimatedForItem = getEstimatedItemSize ? getEstimatedItemSize(data, index, itemType) : void 0;
+  if (typeof estimatedForItem === "number" && Number.isFinite(estimatedForItem)) {
+    size = roundSize(estimatedForItem) + ctx.scrollAxisGap;
+    setSize(ctx, key, size, notifyTotalSize);
+    return size;
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1523,8 +1532,27 @@ function finishMaintainScrollAtEnd(ctx) {
     requestAdjust(ctx, nextScroll - state.scroll);
   }
 }
+// A finger on a web list owns the scroll, as a native drag does. End any
+// programmatic scroll to the end, since MVCP keeps chasing its growing target
+// while one is in flight, and stop following the end until every finger lifts.
+function beginTouchOnList(ctx) {
+  const state = ctx.state;
+  state.isTouchingList = true;
+  const scrollingTo = state.scrollingTo;
+  const isScrollingToEnd = !!scrollingTo && !scrollingTo.animated && !scrollingTo.isInitialScroll && scrollingTo.index === state.props.data.length - 1 && scrollingTo.viewPosition === 1;
+  if (isScrollingToEnd || state.pendingScrollToEnd) {
+    cancelImperativeScroll(state);
+  }
+  if (state.maintainingScrollAtEnd || state.pendingMaintainScrollAtEnd) {
+    finishMaintainScrollAtEnd(ctx);
+  }
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
+  if (state.isTouchingList) {
+    finishMaintainScrollAtEnd(ctx);
+    return false;
+  }
   const {
     didContainersLayout,
     didFinishInitialScroll,
@@ -1747,6 +1775,18 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
   const shouldMVCP = dataChanged ? mvcpData : mvcpScroll;
   const indexByKey = state.indexByKey;
   const prevScroll = state.scroll;
+  // Size changes honor shouldRestorePosition too, not only data changes. When no
+  // row that starts on screen qualifies (a short tail row under a tall one),
+  // fall back to the partially visible first row instead of the rejected row.
+  const canAnchor = (id) => {
+    const index = indexByKey.get(id);
+    if (index === void 0) return false;
+    if (!shouldRestorePosition) return true;
+    const item = state.props.data[index];
+    return item !== void 0 && shouldRestorePosition(item, index, state.props.data);
+  };
+  const partiallyVisibleId = state.startNoBuffer === void 0 || state.startNoBuffer === null || state.startNoBuffer < 0 ? void 0 : state.idCache[state.startNoBuffer];
+  const anchorCandidateIds = partiallyVisibleId === void 0 || partiallyVisibleId === null || idsInView.includes(partiallyVisibleId) ? idsInView : [...idsInView, partiallyVisibleId];
   getContentSize(ctx);
   if (shouldMVCP) {
     if (anchorLock && scrollTarget === void 0) {
@@ -1754,12 +1794,12 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
       prevPosition = anchorLock.position;
     } else if (scrollTarget !== void 0) {
       targetId = getId(state, scrollTarget);
-    } else if (idsInView.length > 0 && state.didContainersLayout && !dataChanged) {
-      targetId = idsInView.find((id) => indexByKey.get(id) !== void 0);
+    } else if (anchorCandidateIds.length > 0 && state.didContainersLayout && !dataChanged) {
+      targetId = anchorCandidateIds.find(canAnchor);
     }
-    if (dataChanged && idsInView.length > 0 && state.didContainersLayout) {
-      for (let i = 0; i < idsInView.length; i++) {
-        const id = idsInView[i];
+    if (dataChanged && anchorCandidateIds.length > 0 && state.didContainersLayout) {
+      for (let i = 0; i < anchorCandidateIds.length; i++) {
+        const id = anchorCandidateIds[i];
         const index = indexByKey.get(id);
         if (index !== void 0) {
           const position = positions[index];
@@ -6338,7 +6378,19 @@ var ListComponentScrollView = React3.forwardRef(function ListComponentScrollView
     if ("onscrollend" in target) {
       target.addEventListener("scrollend", emitScrollEnd);
     }
+    const onTouchStart = () => beginTouchOnList(ctx);
+    const onTouchEnd = (event) => {
+      if (!event.touches || event.touches.length === 0) {
+        ctx.state.isTouchingList = false;
+      }
+    };
+    target.addEventListener("touchstart", onTouchStart, { passive: true });
+    target.addEventListener("touchend", onTouchEnd, { passive: true });
+    target.addEventListener("touchcancel", onTouchEnd, { passive: true });
     return () => {
+      target.removeEventListener("touchstart", onTouchStart);
+      target.removeEventListener("touchend", onTouchEnd);
+      target.removeEventListener("touchcancel", onTouchEnd);
       target.removeEventListener("scroll", handleScroll);
       if ("onscrollend" in target) {
         target.removeEventListener("scrollend", emitScrollEnd);
@@ -6560,8 +6612,11 @@ function ScrollAdjust() {
               window.getComputedStyle(contentNode)[axis.paddingEndProp]
             );
             const temporaryPaddingEnd = `${(currentPaddingEnd || 0) + pad}px`;
-            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: temporaryPaddingEnd };
+            // Record the value the browser actually stored, not the string we wrote:
+            // CSS serialization rounds long decimals (140.46875px -> 140.469px), so a
+            // string compare on the next frame would fail and leave the padding in place.
             contentNode.style[axis.paddingEndProp] = temporaryPaddingEnd;
+            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: contentNode.style[axis.paddingEndProp] };
             void contentNode.offsetHeight;
             scrollBy();
             if (resetPaddingRafRef.current !== void 0) {
@@ -7848,6 +7903,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
     getItemType,
     horizontal,
     rtl,
@@ -8122,6 +8178,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
     getItemType: useWrapIfItem(getItemType),
     hideItemsUntilMeasured: experimental_hideItemsUntilMeasured,
     horizontal: !!horizontal,
diff --git a/react-native.web.mjs b/react-native.web.mjs
index f64169616ea5d761bf82a562084e04f2b98ce7c2..cf1641c53490ae116a767ba63192887d9c6542e5 100644
--- a/react-native.web.mjs
+++ b/react-native.web.mjs
@@ -1202,6 +1202,15 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  // A caller's estimate for an item that has not been measured beats the running
+  // average, which lets one tall kind of row inflate every guess for a short one.
+  const getEstimatedItemSize = state.props.getEstimatedItemSize;
+  const estimatedForItem = getEstimatedItemSize ? getEstimatedItemSize(data, index, itemType) : void 0;
+  if (typeof estimatedForItem === "number" && Number.isFinite(estimatedForItem)) {
+    size = roundSize(estimatedForItem) + ctx.scrollAxisGap;
+    setSize(ctx, key, size, notifyTotalSize);
+    return size;
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1502,8 +1511,27 @@ function finishMaintainScrollAtEnd(ctx) {
     requestAdjust(ctx, nextScroll - state.scroll);
   }
 }
+// A finger on a web list owns the scroll, as a native drag does. End any
+// programmatic scroll to the end, since MVCP keeps chasing its growing target
+// while one is in flight, and stop following the end until every finger lifts.
+function beginTouchOnList(ctx) {
+  const state = ctx.state;
+  state.isTouchingList = true;
+  const scrollingTo = state.scrollingTo;
+  const isScrollingToEnd = !!scrollingTo && !scrollingTo.animated && !scrollingTo.isInitialScroll && scrollingTo.index === state.props.data.length - 1 && scrollingTo.viewPosition === 1;
+  if (isScrollingToEnd || state.pendingScrollToEnd) {
+    cancelImperativeScroll(state);
+  }
+  if (state.maintainingScrollAtEnd || state.pendingMaintainScrollAtEnd) {
+    finishMaintainScrollAtEnd(ctx);
+  }
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
+  if (state.isTouchingList) {
+    finishMaintainScrollAtEnd(ctx);
+    return false;
+  }
   const {
     didContainersLayout,
     didFinishInitialScroll,
@@ -1726,6 +1754,18 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
   const shouldMVCP = dataChanged ? mvcpData : mvcpScroll;
   const indexByKey = state.indexByKey;
   const prevScroll = state.scroll;
+  // Size changes honor shouldRestorePosition too, not only data changes. When no
+  // row that starts on screen qualifies (a short tail row under a tall one),
+  // fall back to the partially visible first row instead of the rejected row.
+  const canAnchor = (id) => {
+    const index = indexByKey.get(id);
+    if (index === void 0) return false;
+    if (!shouldRestorePosition) return true;
+    const item = state.props.data[index];
+    return item !== void 0 && shouldRestorePosition(item, index, state.props.data);
+  };
+  const partiallyVisibleId = state.startNoBuffer === void 0 || state.startNoBuffer === null || state.startNoBuffer < 0 ? void 0 : state.idCache[state.startNoBuffer];
+  const anchorCandidateIds = partiallyVisibleId === void 0 || partiallyVisibleId === null || idsInView.includes(partiallyVisibleId) ? idsInView : [...idsInView, partiallyVisibleId];
   getContentSize(ctx);
   if (shouldMVCP) {
     if (anchorLock && scrollTarget === void 0) {
@@ -1733,12 +1773,12 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
       prevPosition = anchorLock.position;
     } else if (scrollTarget !== void 0) {
       targetId = getId(state, scrollTarget);
-    } else if (idsInView.length > 0 && state.didContainersLayout && !dataChanged) {
-      targetId = idsInView.find((id) => indexByKey.get(id) !== void 0);
+    } else if (anchorCandidateIds.length > 0 && state.didContainersLayout && !dataChanged) {
+      targetId = anchorCandidateIds.find(canAnchor);
     }
-    if (dataChanged && idsInView.length > 0 && state.didContainersLayout) {
-      for (let i = 0; i < idsInView.length; i++) {
-        const id = idsInView[i];
+    if (dataChanged && anchorCandidateIds.length > 0 && state.didContainersLayout) {
+      for (let i = 0; i < anchorCandidateIds.length; i++) {
+        const id = anchorCandidateIds[i];
         const index = indexByKey.get(id);
         if (index !== void 0) {
           const position = positions[index];
@@ -6317,7 +6357,19 @@ var ListComponentScrollView = forwardRef(function ListComponentScrollView2({
     if ("onscrollend" in target) {
       target.addEventListener("scrollend", emitScrollEnd);
     }
+    const onTouchStart = () => beginTouchOnList(ctx);
+    const onTouchEnd = (event) => {
+      if (!event.touches || event.touches.length === 0) {
+        ctx.state.isTouchingList = false;
+      }
+    };
+    target.addEventListener("touchstart", onTouchStart, { passive: true });
+    target.addEventListener("touchend", onTouchEnd, { passive: true });
+    target.addEventListener("touchcancel", onTouchEnd, { passive: true });
     return () => {
+      target.removeEventListener("touchstart", onTouchStart);
+      target.removeEventListener("touchend", onTouchEnd);
+      target.removeEventListener("touchcancel", onTouchEnd);
       target.removeEventListener("scroll", handleScroll);
       if ("onscrollend" in target) {
         target.removeEventListener("scrollend", emitScrollEnd);
@@ -6539,8 +6591,11 @@ function ScrollAdjust() {
               window.getComputedStyle(contentNode)[axis.paddingEndProp]
             );
             const temporaryPaddingEnd = `${(currentPaddingEnd || 0) + pad}px`;
-            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: temporaryPaddingEnd };
+            // Record the value the browser actually stored, not the string we wrote:
+            // CSS serialization rounds long decimals (140.46875px -> 140.469px), so a
+            // string compare on the next frame would fail and leave the padding in place.
             contentNode.style[axis.paddingEndProp] = temporaryPaddingEnd;
+            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: contentNode.style[axis.paddingEndProp] };
             void contentNode.offsetHeight;
             scrollBy();
             if (resetPaddingRafRef.current !== void 0) {
@@ -7827,6 +7882,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
     getItemType,
     horizontal,
     rtl,
@@ -8101,6 +8157,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
     getItemType: useWrapIfItem(getItemType),
     hideItemsUntilMeasured: experimental_hideItemsUntilMeasured,
     horizontal: !!horizontal,
diff --git a/react.d.ts b/react.d.ts
index c424d3ce576ea8205713d2e34bc5c0ad6de34db9..9f2abe8b8ec7fc4bbbefad87768591eb7dd9ddaa 100644
--- a/react.d.ts
+++ b/react.d.ts
@@ -224,6 +224,11 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Estimates the size of an item that has not been measured yet, in place of the running
+     * average of measured items. Return undefined to fall back to the average.
+     */
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
diff --git a/react.js b/react.js
index 5b7b716e1edb046cbb6386b417490fab793f666b..34b79d7de27f5ae9e4f4b6fc4a8879c5d705560f 100644
--- a/react.js
+++ b/react.js
@@ -1223,6 +1223,15 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  // A caller's estimate for an item that has not been measured beats the running
+  // average, which lets one tall kind of row inflate every guess for a short one.
+  const getEstimatedItemSize = state.props.getEstimatedItemSize;
+  const estimatedForItem = getEstimatedItemSize ? getEstimatedItemSize(data, index, itemType) : void 0;
+  if (typeof estimatedForItem === "number" && Number.isFinite(estimatedForItem)) {
+    size = roundSize(estimatedForItem) + ctx.scrollAxisGap;
+    setSize(ctx, key, size, notifyTotalSize);
+    return size;
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1523,8 +1532,27 @@ function finishMaintainScrollAtEnd(ctx) {
     requestAdjust(ctx, nextScroll - state.scroll);
   }
 }
+// A finger on a web list owns the scroll, as a native drag does. End any
+// programmatic scroll to the end, since MVCP keeps chasing its growing target
+// while one is in flight, and stop following the end until every finger lifts.
+function beginTouchOnList(ctx) {
+  const state = ctx.state;
+  state.isTouchingList = true;
+  const scrollingTo = state.scrollingTo;
+  const isScrollingToEnd = !!scrollingTo && !scrollingTo.animated && !scrollingTo.isInitialScroll && scrollingTo.index === state.props.data.length - 1 && scrollingTo.viewPosition === 1;
+  if (isScrollingToEnd || state.pendingScrollToEnd) {
+    cancelImperativeScroll(state);
+  }
+  if (state.maintainingScrollAtEnd || state.pendingMaintainScrollAtEnd) {
+    finishMaintainScrollAtEnd(ctx);
+  }
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
+  if (state.isTouchingList) {
+    finishMaintainScrollAtEnd(ctx);
+    return false;
+  }
   const {
     didContainersLayout,
     didFinishInitialScroll,
@@ -1747,6 +1775,18 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
   const shouldMVCP = dataChanged ? mvcpData : mvcpScroll;
   const indexByKey = state.indexByKey;
   const prevScroll = state.scroll;
+  // Size changes honor shouldRestorePosition too, not only data changes. When no
+  // row that starts on screen qualifies (a short tail row under a tall one),
+  // fall back to the partially visible first row instead of the rejected row.
+  const canAnchor = (id) => {
+    const index = indexByKey.get(id);
+    if (index === void 0) return false;
+    if (!shouldRestorePosition) return true;
+    const item = state.props.data[index];
+    return item !== void 0 && shouldRestorePosition(item, index, state.props.data);
+  };
+  const partiallyVisibleId = state.startNoBuffer === void 0 || state.startNoBuffer === null || state.startNoBuffer < 0 ? void 0 : state.idCache[state.startNoBuffer];
+  const anchorCandidateIds = partiallyVisibleId === void 0 || partiallyVisibleId === null || idsInView.includes(partiallyVisibleId) ? idsInView : [...idsInView, partiallyVisibleId];
   getContentSize(ctx);
   if (shouldMVCP) {
     if (anchorLock && scrollTarget === void 0) {
@@ -1754,12 +1794,12 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
       prevPosition = anchorLock.position;
     } else if (scrollTarget !== void 0) {
       targetId = getId(state, scrollTarget);
-    } else if (idsInView.length > 0 && state.didContainersLayout && !dataChanged) {
-      targetId = idsInView.find((id) => indexByKey.get(id) !== void 0);
+    } else if (anchorCandidateIds.length > 0 && state.didContainersLayout && !dataChanged) {
+      targetId = anchorCandidateIds.find(canAnchor);
     }
-    if (dataChanged && idsInView.length > 0 && state.didContainersLayout) {
-      for (let i = 0; i < idsInView.length; i++) {
-        const id = idsInView[i];
+    if (dataChanged && anchorCandidateIds.length > 0 && state.didContainersLayout) {
+      for (let i = 0; i < anchorCandidateIds.length; i++) {
+        const id = anchorCandidateIds[i];
         const index = indexByKey.get(id);
         if (index !== void 0) {
           const position = positions[index];
@@ -6338,7 +6378,19 @@ var ListComponentScrollView = React3.forwardRef(function ListComponentScrollView
     if ("onscrollend" in target) {
       target.addEventListener("scrollend", emitScrollEnd);
     }
+    const onTouchStart = () => beginTouchOnList(ctx);
+    const onTouchEnd = (event) => {
+      if (!event.touches || event.touches.length === 0) {
+        ctx.state.isTouchingList = false;
+      }
+    };
+    target.addEventListener("touchstart", onTouchStart, { passive: true });
+    target.addEventListener("touchend", onTouchEnd, { passive: true });
+    target.addEventListener("touchcancel", onTouchEnd, { passive: true });
     return () => {
+      target.removeEventListener("touchstart", onTouchStart);
+      target.removeEventListener("touchend", onTouchEnd);
+      target.removeEventListener("touchcancel", onTouchEnd);
       target.removeEventListener("scroll", handleScroll);
       if ("onscrollend" in target) {
         target.removeEventListener("scrollend", emitScrollEnd);
@@ -6560,8 +6612,11 @@ function ScrollAdjust() {
               window.getComputedStyle(contentNode)[axis.paddingEndProp]
             );
             const temporaryPaddingEnd = `${(currentPaddingEnd || 0) + pad}px`;
-            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: temporaryPaddingEnd };
+            // Record the value the browser actually stored, not the string we wrote:
+            // CSS serialization rounds long decimals (140.46875px -> 140.469px), so a
+            // string compare on the next frame would fail and leave the padding in place.
             contentNode.style[axis.paddingEndProp] = temporaryPaddingEnd;
+            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: contentNode.style[axis.paddingEndProp] };
             void contentNode.offsetHeight;
             scrollBy();
             if (resetPaddingRafRef.current !== void 0) {
@@ -7848,6 +7903,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
     getItemType,
     horizontal,
     rtl,
@@ -8122,6 +8178,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
     getItemType: useWrapIfItem(getItemType),
     hideItemsUntilMeasured: experimental_hideItemsUntilMeasured,
     horizontal: !!horizontal,
diff --git a/react.mjs b/react.mjs
index f64169616ea5d761bf82a562084e04f2b98ce7c2..cf1641c53490ae116a767ba63192887d9c6542e5 100644
--- a/react.mjs
+++ b/react.mjs
@@ -1202,6 +1202,15 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  // A caller's estimate for an item that has not been measured beats the running
+  // average, which lets one tall kind of row inflate every guess for a short one.
+  const getEstimatedItemSize = state.props.getEstimatedItemSize;
+  const estimatedForItem = getEstimatedItemSize ? getEstimatedItemSize(data, index, itemType) : void 0;
+  if (typeof estimatedForItem === "number" && Number.isFinite(estimatedForItem)) {
+    size = roundSize(estimatedForItem) + ctx.scrollAxisGap;
+    setSize(ctx, key, size, notifyTotalSize);
+    return size;
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1502,8 +1511,27 @@ function finishMaintainScrollAtEnd(ctx) {
     requestAdjust(ctx, nextScroll - state.scroll);
   }
 }
+// A finger on a web list owns the scroll, as a native drag does. End any
+// programmatic scroll to the end, since MVCP keeps chasing its growing target
+// while one is in flight, and stop following the end until every finger lifts.
+function beginTouchOnList(ctx) {
+  const state = ctx.state;
+  state.isTouchingList = true;
+  const scrollingTo = state.scrollingTo;
+  const isScrollingToEnd = !!scrollingTo && !scrollingTo.animated && !scrollingTo.isInitialScroll && scrollingTo.index === state.props.data.length - 1 && scrollingTo.viewPosition === 1;
+  if (isScrollingToEnd || state.pendingScrollToEnd) {
+    cancelImperativeScroll(state);
+  }
+  if (state.maintainingScrollAtEnd || state.pendingMaintainScrollAtEnd) {
+    finishMaintainScrollAtEnd(ctx);
+  }
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
+  if (state.isTouchingList) {
+    finishMaintainScrollAtEnd(ctx);
+    return false;
+  }
   const {
     didContainersLayout,
     didFinishInitialScroll,
@@ -1726,6 +1754,18 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
   const shouldMVCP = dataChanged ? mvcpData : mvcpScroll;
   const indexByKey = state.indexByKey;
   const prevScroll = state.scroll;
+  // Size changes honor shouldRestorePosition too, not only data changes. When no
+  // row that starts on screen qualifies (a short tail row under a tall one),
+  // fall back to the partially visible first row instead of the rejected row.
+  const canAnchor = (id) => {
+    const index = indexByKey.get(id);
+    if (index === void 0) return false;
+    if (!shouldRestorePosition) return true;
+    const item = state.props.data[index];
+    return item !== void 0 && shouldRestorePosition(item, index, state.props.data);
+  };
+  const partiallyVisibleId = state.startNoBuffer === void 0 || state.startNoBuffer === null || state.startNoBuffer < 0 ? void 0 : state.idCache[state.startNoBuffer];
+  const anchorCandidateIds = partiallyVisibleId === void 0 || partiallyVisibleId === null || idsInView.includes(partiallyVisibleId) ? idsInView : [...idsInView, partiallyVisibleId];
   getContentSize(ctx);
   if (shouldMVCP) {
     if (anchorLock && scrollTarget === void 0) {
@@ -1733,12 +1773,12 @@ function prepareMVCP(ctx, dataChanged, adjustmentSource) {
       prevPosition = anchorLock.position;
     } else if (scrollTarget !== void 0) {
       targetId = getId(state, scrollTarget);
-    } else if (idsInView.length > 0 && state.didContainersLayout && !dataChanged) {
-      targetId = idsInView.find((id) => indexByKey.get(id) !== void 0);
+    } else if (anchorCandidateIds.length > 0 && state.didContainersLayout && !dataChanged) {
+      targetId = anchorCandidateIds.find(canAnchor);
     }
-    if (dataChanged && idsInView.length > 0 && state.didContainersLayout) {
-      for (let i = 0; i < idsInView.length; i++) {
-        const id = idsInView[i];
+    if (dataChanged && anchorCandidateIds.length > 0 && state.didContainersLayout) {
+      for (let i = 0; i < anchorCandidateIds.length; i++) {
+        const id = anchorCandidateIds[i];
         const index = indexByKey.get(id);
         if (index !== void 0) {
           const position = positions[index];
@@ -6317,7 +6357,19 @@ var ListComponentScrollView = forwardRef(function ListComponentScrollView2({
     if ("onscrollend" in target) {
       target.addEventListener("scrollend", emitScrollEnd);
     }
+    const onTouchStart = () => beginTouchOnList(ctx);
+    const onTouchEnd = (event) => {
+      if (!event.touches || event.touches.length === 0) {
+        ctx.state.isTouchingList = false;
+      }
+    };
+    target.addEventListener("touchstart", onTouchStart, { passive: true });
+    target.addEventListener("touchend", onTouchEnd, { passive: true });
+    target.addEventListener("touchcancel", onTouchEnd, { passive: true });
     return () => {
+      target.removeEventListener("touchstart", onTouchStart);
+      target.removeEventListener("touchend", onTouchEnd);
+      target.removeEventListener("touchcancel", onTouchEnd);
       target.removeEventListener("scroll", handleScroll);
       if ("onscrollend" in target) {
         target.removeEventListener("scrollend", emitScrollEnd);
@@ -6539,8 +6591,11 @@ function ScrollAdjust() {
               window.getComputedStyle(contentNode)[axis.paddingEndProp]
             );
             const temporaryPaddingEnd = `${(currentPaddingEnd || 0) + pad}px`;
-            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: temporaryPaddingEnd };
+            // Record the value the browser actually stored, not the string we wrote:
+            // CSS serialization rounds long decimals (140.46875px -> 140.469px), so a
+            // string compare on the next frame would fail and leave the padding in place.
             contentNode.style[axis.paddingEndProp] = temporaryPaddingEnd;
+            temporaryPaddingRef.current = { baseline: baselinePaddingEnd, value: contentNode.style[axis.paddingEndProp] };
             void contentNode.offsetHeight;
             scrollBy();
             if (resetPaddingRafRef.current !== void 0) {
@@ -7827,6 +7882,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
     getItemType,
     horizontal,
     rtl,
@@ -8101,6 +8157,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
     getItemType: useWrapIfItem(getItemType),
     hideItemsUntilMeasured: experimental_hideItemsUntilMeasured,
     horizontal: !!horizontal,
